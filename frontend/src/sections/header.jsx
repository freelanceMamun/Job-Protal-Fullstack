import React from 'react';
import Navbar from '@/components/navbar';
const Header = () => {
  return (
    <header className="sticky lg:static top-0">
      <Navbar></Navbar>
    </header>
  );
};

export default Header;
